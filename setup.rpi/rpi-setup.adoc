= RPI Setup Guide


[%collapsible, title=Codeblocks]
====
----
curl -sSL https://install.pi-hole.net | bash && \
curl -s https://syncthing.net/release-key.txt | sudo apt-key add - && \
echo "deb https://apt.syncthing.net/ syncthing stable" | sudo tee /etc/apt/sources.list.d/syncthing.list && \
curl -fsSL https://get.docker.com | sh
----
----
sudo apt update && sudo apt install -y \
unbound \
ufw \
nginx \
dnsmasq \
hostapd \
bluez \
vlc \
syncthing \
fail2ban \
rkhunter \
chkrootkit \
wireguard
----
====


[cols="2%,30%,68%",options="header"]
|===
|# |Prereq |Extra
|01 |Update system and enable SSH |`sudo apt update && sudo apt upgrade -y && sudo raspi-config`
|02 |Static IP + Hostname |Edit `/etc/dhcpcd.conf`, set static IP. Set hostname via `raspi-config` > Network Options.
|03 |Install Pi-hole |`curl -sSL https://install.pi-hole.net \| bash`
|04 |Custom DNS filtering (RethinkDNS) |Export filters from RethinkDNS to `hosts` format, add to Pi-hole blacklist.
|05 |Set up DNS server | `sudo apt install unbound` 
|06 |Enable DHCP via Pi-hole |Web UI: Settings > DHCP > Enable. Disable DHCP on router.
|07 |Configure firewall (UFW) |`sudo apt install ufw && sudo ufw default deny incoming && sudo ufw allow ssh && sudo ufw allow 53,80,443/tcp && sudo ufw enable`
|08 |Deploy local server (Flask/Node.js/Nginx) |`sudo apt install nginx`
|09 |Connect RPi to PC via USB Ethernet gadget |Edit `/boot/config.txt` and `/boot/cmdline.txt`, configure USB OTG in `/etc/network/interfaces`.
|10 |Share RPi's Wi-Fi + Bluetooth |Install `dnsmasq`, `hostapd`, `bluez`, setup NAT, enable IP forwarding in `/etc/sysctl.conf`.
|11 |HDMI output to TV |Configure `/boot/config.txt`. `sudo apt install vlc`, use `chromium`, `spotifyd`, or `motrix`.
|12 |Install Syncthing (LAN) |Add repo and `sudo apt install syncthing`
|13 |Syncthing (Remote Access) |Allow ports, use Tailscale/ZeroTier.
|14 |Hardening: Fail2Ban, firewall rules |`sudo apt install fail2ban`, configure `jail.local`, use rkhunter
|15 |Data storage |Mount SSD via `/etc/fstab`, use `rsync`, `borgbackup`, or `restic`
|16 |Extra: VPN (WireGuard) |`sudo apt install wireguard`, configure and enable service.
|17 |Extra: Docker + Portainer |Install Docker, run Portainer container.
|===


[%collapsible, title=Prompt]
====
....
== RPI Setup
you will be providing expertise and creating step-by-step pill-sized guide.

=== desired outcome
I want my 'raspberry pi 5' to have following functions & utilities:

* pie hole
* DNS server - with custom filtering rules (extracted from 'rethink DNS') 
* DHCP
* firewall
* deployment sever (LAN)
* directly connected to PC, sharing RPI's bluetooth and Wi-Fi capabilities
* self-hosted server
* Direct connection to TV - media sttram from RPI: vlc, spotify, YouTube, motrix
* syncthing on LAN
* syncthing full remote
* any other solutions for enhancing security, data storage, or bypassing lifes limitations

=== reality check
Is it possible and feasible to achieve all of that on a single Raspberry Pi device?
Answer: <Y/N>?

==== if {"yes"}
Create overarching step by step guide, formatted as a table, with columns for index (width = 2% of total table width), prerequisites, and a one-liner commands or download links column

==== if {"no"}
1. list exclusions
2. create trimmed-down version for a most versatile, feasible and secure solution
3. Create overarching step by step guide, formatted as a table, with columns for index (width = 2% of total table width), prerequisites, and a one-liner commands or download links column
....
====


[%collapsible, title=Overview]
====
This guide enables a Raspberry Pi 5 to function as a: 
 
* Pi-hole DNS + DHCP server with custom filtering
* LAN deployment server
* Local media streaming station
* Syncthing node (LAN & remote)
* Firewall, VPN node, and connectivity bridge
====
